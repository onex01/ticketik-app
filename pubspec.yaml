name: ticketik_app
description: "Тикетик – быстрая отправка заявок в техподдержку"
publish_to: 'none'

version: 0.8.5+1

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter
  # Firebase
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4
  firebase_messaging: ^15.1.3
  # Утилиты
  network_info_plus: ^6.1.0
  device_info_plus: ^10.1.2
  uuid: ^4.5.1
  shared_preferences: ^2.3.2
  intl: ^0.19.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_distributor: ^0.0.2

flutter:
  uses-material-design: true
